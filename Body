    <style>
        /* ... existing body, chat-container, #chat-box styles ... */

        /* Container for AI Message (holds icon + bubble) */
        .ai-message-container {
            display: flex; /* Aligns child items (icon and message) horizontally */
            align-items: flex-start; /* Aligns items to the top */
            margin-bottom: 15px;
        }

        /* GHOST AI Icon Styling */
        .ai-icon {
            width: 40px; /* Size of the avatar */
            height: 40px;
            border-radius: 50%; /* Make it circular */
            margin-right: 10px;
            background-color: #8a2be2; /* Match AI purple */
            /* This is where the image will be placed */
            background-image: url('{{ url_for("static", filename="ghost-icon.png") }}');
            background-size: cover;
            flex-shrink: 0; /* Prevents the icon from shrinking */
        }
        
        /* Message bubble styles remain similar */
        .message {
            padding: 8px 12px;
            border-radius: 15px;
            max-width: 70%;
            line-height: 1.4;
            /* Remove margin-bottom from .message as container handles spacing */
            margin-bottom: 0; 
        }
        
        /* User message still aligns right */
        .user-message {
            background-color: #4CAF50;
            margin-left: auto; /* Pushes user message to the right */
            color: white;
            text-align: right;
        }

        /* AI message only needs background/color, container handles alignment */
        .ai-message {
            background-color: #2b2b45; /* Darker than the container for contrast */
            color: white;
            text-align: left;
        }

        /* ... existing #input-form, #user-input, #send-button styles ... */
    </style>
